# cnn-intel-image-classification
Image classification project using a Convolutional Neural Network (CNN) built with Keras/TensorFlow. The model is trained on the Intel Image Classification dataset to classify natural scenes into 6 categories: buildings, forest, glacier, mountain, sea, and street.
